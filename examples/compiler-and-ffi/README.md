# Compiler, IR, and FFI

This directory hosts low-level examples demonstrating the TISC Intermediate Representation, C API bindings (`t81_core.h`, `main.c`), and standard CMake consumer setups.

These files test the core deterministic foundation and compiler boundaries of the T81 runtime.
