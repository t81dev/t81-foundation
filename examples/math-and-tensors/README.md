# Math and Tensors

This directory contains examples of basic and advanced mathematical operations in T81, focusing on native data types, matrix multiplication, and graph algorithms.

To run these `.t81` scripts, execute them using the compiled T81 binary from your build directory. For example:

```bash
../../build/t81 run vector_demo.t81
```