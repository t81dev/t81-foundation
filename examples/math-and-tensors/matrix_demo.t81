fn matrix_demo() -> i32 {
    // Matrix literals are not enabled in the current frontend; use vectors.
    let row0: T81Vector[i32, 2] = [1, 2];
    let row1: T81Vector[i32, 2] = [3, 4];
    let trace: i32 = row0[0] + row1[1];
    return trace;
}

fn main() -> i32 {
    return matrix_demo();
}
