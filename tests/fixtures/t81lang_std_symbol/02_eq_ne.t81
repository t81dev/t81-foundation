fn main() -> i32 {
  let a: Symbol = std.symbol.intern("omega");
  let b: Symbol = std.symbol.intern("omega");
  let c: Symbol = std.symbol.intern("alpha");
  print(std.symbol.eq(a, b));
  print(std.symbol.eq(a, c));
  print(std.symbol.ne(a, c));
  print(std.symbol.ne(a, b));
  return 0;
}
