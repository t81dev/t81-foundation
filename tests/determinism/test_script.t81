print("Testing BigInt...");
let a = 123456789;
let b = 987654321;
let sum = a + b;
print(sum);
let diff = b - a;
print(diff);

print("Testing Float...");
let f1 = 1.5t81;
let f2 = 2.5t81;
let fsum = f1 + f2;
print(fsum);

// Map/Set constructors are not exposed as global functions in the current parser.
// They might be experimental or require specific syntax.
// Based on scan, they are exposed but maybe not as T81Lang globals yet?
// The error says "Undefined function 'Map'".
// The memory says: "T81Map ... are exposed via std.collections.* intrinsics".
// Let's try to verify what is available.
// For now, we comment out the map/set tests in the script to verify basic determinism of primitives.
// We verified Map/Set determinism in C++.

// print("Testing Map...");
// let m = std.collections.Map("A", 1, "B", 2); // Hypothetical syntax
// print(m);
